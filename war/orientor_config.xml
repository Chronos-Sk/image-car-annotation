<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE wire-frame SYSTEM "config.dtd">
<config>
	<wire-frames>
		<!-- Relative URI's start from the script directory. -->
		<wire-frame name="Sedan" default="true">../models/sedan.objm</wire-frame>
		<wire-frame name="Hatchback">../models/hatchback.objm</wire-frame>
		<wire-frame name="Pickup">../models/pickup.objm</wire-frame>
		<wire-frame name="SUV">../models/suv.objm</wire-frame>
		<wire-frame name="Van">../models/van.objm</wire-frame>
	</wire-frames>
	<form>
		<method>GET</method>
		<action>/hallo.html</action>
		<target>_self</target>
	</form>
</config>